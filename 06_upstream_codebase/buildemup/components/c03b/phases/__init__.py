"""C3b phase pipeline (α → β → γ → δ → ε → ζ).

Per spec § 3:
  α canonicalization + applicability   → Phase α
  β tweak generation per layout         → Phase β
  γ cost + space + comfort impact      → Phase γ
  δ user-facing presentation rendering  → Phase δ
  ε user-turn handling                  → Phase ε
  ζ resolution + handoff signing        → Phase ζ
"""
